MessageSystem
Server.cs       -- apply it to export the execute file to make a server which is used to connect all client
ClientEvent.cs  -- apply it to expoer the execute file to each client user, handling the event action

TextShow.cs 	-- show some info. in when playing

Vuforia
DefaultInitializationErrorHanfler.cs
DefaultTrackableEventHandler.cs   -- handling the event when track the AR

script_D19668.cs --  view each game object and record the position/rotation/scaling info. into a .xml file 
		with the obj structure (parent/children...)
		((there is also include UnityWebRequest that upload some info. to the web server??

WebRequest.cs 	-- socket -- UDP info. transfer
 		((there is also include UnityWebRequest that upload some info. to the web server??